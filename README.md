# 📝 Quote Sender Microservice API

 Микросервисное REST-приложение для создания и оценки цитат. Посетителям доступен просмотр цитат, аторизованные пользователи могут публиковать цитаты, редактировать их и голосовать за цитаты пользователей.  

---

## 🚀 Основные технологии

- **Java 21**, **Spring Boot 3**, **Spring Cloud**
- **Eureka Server** — сервис-дискавери  
- **API Gateway** — маршрутизация и фильтрация запросов  
- **JWT Authentication** — безопасность и аутентификация  
- **MySQL** — база данных  
- **Docker & Docker Compose** — контейнеризация и деплой  
- **GitHub Actions (CI/CD)** — автоматическая сборка и тестирование  

---

## ⚙️ Архитектура проекта

Проект состоит из пяти микросервисов:

| Сервис | Порт | Назначение |
|--------|------|-------------|
| **Eureka Server** | 8761 | Регистрация и обнаружение сервисов |
| **API Gateway** | 8080 | Единая точка входа в систему |
| **User Service** | 8081 | Регистрация, аутентификация, управление пользователями |
| **Quote Service** | 8082 | CRUD-операции с цитатами |
| **Vote Service** | 8083 | Голосование за цитаты (лайки/дизлайки) |

---

## 🧰 Предварительные требования

Перед запуском убедитесь, что установлены:
- **JDK 21+**
- **Maven 3.9+**
- **Docker Desktop**
- **Git**

---

## 🐳 Запуск проекта

### 1️⃣ Клонировать репозиторий:
```bash
git clone https://github.com/Bilsher/quoteapi-microservice.git
cd quoteapi-microservice 
```

### 2️⃣ Собрать проект Maven:
```bash
mvn clean package -DskipTests
```

### 3️⃣ Запустить сервисы через Docker Compose:
```bash
docker-compose up --build
```

### 4️⃣ Проверить, что всё работает:
Eureka Dashboard: http://localhost:8761
API Gateway (точка входа): http://localhost:8080

После запуска все сервисы автоматически зарегистрируются в Eureka.

---

### Взаимодействия с API
🔐 Регистрация пользователя
POST http://localhost:8080/api/v1/auth/register
```bash
{
  "username": "john",
  "password": "123456"
}
```
🔑 Аутентификация
POST http://localhost:8080/api/v1/auth/login
```bash
{
  "username": "john",
  "password": "123456"
}
```
Login response:
```bash
  "token": "eyJhbGciOiJIUzI1NiJ9..."
```
Используйте токен в заголовках всех запросов:
```bash
Authorization: Bearer <token>
```
✍️ Создать цитату
POST http://localhost:8080/api/v1/quotes/create
```bash
{
  "quoteText": "Test quote."
}
```

📃 Получить все цитаты
GET http://localhost:8080/api/v1/quotes/all

❤️ Поставить лайк цитате

POST http://localhost:8080/api/v1/votes/{quoteId}/like

👎 Поставить дизлайк цитате

POST http://localhost:8080/api/v1/votes/{quoteId}/dislike

🧮 Получить топ цитат

GET http://localhost:8080/api/v1/quotes/top




